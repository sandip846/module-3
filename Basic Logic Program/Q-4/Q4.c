#include <stdio.h>
int main()
{
	float area,side;
	printf("\n Enter the length of the side of the square= ");
	scanf("%f",&side);
	
	area=side*side;
	
	printf("\n the area of squre is=%.2f",area);
	return 0;
}
